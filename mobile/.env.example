# Android emulator → host machine API (NOT localhost)
# Dev: HTTP + adb reverse (debug build allows cleartext via debug manifest).
EXPO_PUBLIC_API_URL=http://10.0.2.2:8000

# Production release (CI / local release build — HTTPS only, no trailing slash):
# EXPO_PUBLIC_API_URL=https://api.your-domain.com
